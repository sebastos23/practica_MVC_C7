<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %>
<body>
    <h1> <%= title %> </h1>

    <% autos.forEach(auto => { %>
        
    <ul>
        <li> <%= auto.marca %> </li>
        <li> <%= auto.modelo %> </li>
        <li> <%= auto.anio %> </li>
        <li> <%= auto.color %> </li>
    </ul>

    <% }); %> 
    
</body>
</html>